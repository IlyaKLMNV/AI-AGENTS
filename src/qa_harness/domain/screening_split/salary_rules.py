"""Данные пересчёта зарплаты. Меняя любое значение — поднимай `SALARY_RULES_VERSION`: по ней
воспроизводится старое решение из аудит-записи диалога.
"""

SALARY_RULES_VERSION = "2026-08-27"

# Валюта вне справочника приходит от Аналитика как `other` → уточняем у кандидата.
RATE_TO_RUB = {"RUB": 1.0, "USD": 80.0, "EUR": 90.0, "KZT": 0.2}

HOURS_PER_MONTH = 160  # день = 8 ч, неделя = 40 ч
PERIOD_TO_MONTH = {
    "month": 1.0,
    "hour": float(HOURS_PER_MONTH),
    "day": HOURS_PER_MONTH / 8,
    "week": HOURS_PER_MONTH / 40,
    "year": 1 / 12,
}

# НДФЛ: ставки предельные, порог по ГОДОВОМУ доходу, None = «и выше». Вычеты не учитываем.
# Шкала с 2025, в 2026 параметры не менялись (сверено 2026-08-27).
NDFL_BRACKETS = (
    (2_400_000, 0.13),
    (5_000_000, 0.15),
    (20_000_000, 0.18),
    (50_000_000, 0.20),
    (None, 0.22),
)

# Голая сумма меньше этого читается как тысячи: «70» = 70000, «250000» = 250000.
BARE_THOUSANDS_BELOW = 1000
