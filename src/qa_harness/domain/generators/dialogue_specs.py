"""Спеки генерации диалогов под вердикт: подсказки по вердиктам + marker-константы валидации.

Перенесено дословно из verdict_classifier_runner (SCENARIO_HINTS_BY_VERDICT, marker-кортежи,
_pick_scenario_hint).
"""

from __future__ import annotations

import random
from typing import Dict, List, Optional

SCENARIO_HINTS_BY_VERDICT: Dict[str, List[str]] = {
    "passed": [
        "Нормальный скрининг по алгоритму, кандидат отвечает по делу, рекрутер завершает END после получения всех ответов.",
    ],
    "failed": [
        "KO по зарплате: кандидат называет ожидания выше бюджета из контекста, рекрутер вежливо отказывает и END.",
        "KO по локации для офис/гибрид: кандидат не в городе вакансии и не готов к переезду, рекрутер отказывает и END.",
        "Нет нужного опыта: кандидат прямо говорит, что опыта/стека нет, рекрутер отказывает и END.",
        "Кандидат отказывается от вакансии по сути: 'не интересно/не актуально/не рассматриваю', рекрутер завершает END.",
        "Агрессия/оскорбления/флирт/политика/просьба денег/тестовое - рекрутер завершает по скрипту и END (это failed по сути отказа).",
        "Кандидат уже трудоустроен/принял оффер - рекрутер завершает END (failed).",
        "Вопрос 'вы бот/ИИ?'",
    ],
    "deadlock": [
        "Не тот человек/ошибка контакта: кандидат говорит, что это не он или вы ошиблись контактом, скрининг по сути не начинается, END.",
        "Неразборчиво два раза: рекрутер один раз уточняет.",
        "Иностранный язык так, что скрининг не идет: коммуникация срывается из-за языка, но не из-за явного несоответствия требованиям вакансии. END.",
        "Источник контакта/легитимность/корпоративная почта: кандидат требует подтверждений и не идет по скринингу, диалог заканчивается END.",
    ],
}

FORBIDDEN_RECRUITER_STYLE_MARKERS = (
    "вы подходите",
    "отлично подходите",
    "ваш профиль подходит",
    "ваш профиль отлично подходит",
    "подходите на эту позицию",
    "подходите по опыту",
    "зарплатный диапазон",
    "наш бюджет",
    "вилка",
    "бюджет для этой позиции",
)
FOREIGN_LANGUAGE_DEADLOCK_BAD_MARKERS = (
    "уровень английского",
    "английского не ниже",
    "необходимы базовые навыки английского",
    "не подходит из-за английского",
    "не подойдете из-за английского",
)
WRONG_CONTACT_DEADLOCK_REQUIRED_MARKERS = (
    "не тот человек",
    "ошиблись номером",
    "ошиблись контактом",
    "это не я",
    "не по адресу",
    "написали не тому человеку",
)
WRONG_CONTACT_DEADLOCK_BAD_MARKERS = (
    "уже общались",
    "уже проходил",
    "повторно",
    "не пишите мне повторно",
    "второй раз",
    "неактуален процесс",
    "по этой вакансии уже",
)


def pick_verdict_hint(
    target_verdict: str,
    rng: random.Random,
    scenario_mode: str,
    scenario_count_per_verdict: Optional[int],
    cycle_state: Optional[Dict[str, int]] = None,
) -> str:
    pool = SCENARIO_HINTS_BY_VERDICT.get(target_verdict) or ["Нормальный диалог."]
    if scenario_count_per_verdict is not None and 0 < scenario_count_per_verdict < len(pool):
        pool = pool[:scenario_count_per_verdict]

    if scenario_mode == "random":
        return rng.choice(pool)

    if cycle_state is None:
        cycle_state = {}
    idx = int(cycle_state.get(target_verdict, 0))
    cycle_state[target_verdict] = idx + 1
    return pool[idx % len(pool)]
