# Кросс-репный план split-скрининга

> **Статус на 31.08.2026.** В файле только **незакрытая работа**. Всё остальное лежит отдельно:
> состояние репозиториев, ветки и коммиты — [../repos.md](../repos.md); механика действующего движка и
> счётчики — [mechanics_decision.html](mechanics_decision.html); механика нового —
> [mechanics_policy.html](mechanics_policy.html); принятые решения (Р1–Р14, Д1–Д7) —
> [decisions_rearchitecture.md](decisions_rearchitecture.md) (плюс Р15–Р17); как читать отчёт и как гонять —
> [report_analysis.md](report_analysis.md); два движка рядом —
> [handoff_two_engines.md](handoff_two_engines.md); контекст hh-порта (карта `eggplant-api`, отличия
> каналов, порядок работ) — [handoff_eggplant.md](handoff_eggplant.md). Доказательства по закрытым
> пунктам — в журналах `review_<дата>.md`.

Файл живёт до внедрения: закрывая пункт, доказательство переносим в журнал прогона или в журнал
решений, а сам пункт отсюда удаляем.

---

## Блокирует релиз нового движка

1. **Разобрать 9 провалов вариативного прогона** `20260829_163434` (`--input-mode generated`,
   77 сценариев × 2 варианта = 154 кейса, 145 прошли; стек A:v3 + I:v2). Классы разные, часть
   наверняка дрейф генератора, но ни один не разобран:
   - **Интервьюер, сценарий 28** — раскрыл название компании при скрытом поиске. Единственный
     кандидат в реальную утечку, смотреть первым;
   - **Интервьюер, сценарий 66 (оба варианта)** — рубрика судьи, пункт 2; сценарий про ссылку;
   - **Аналитик, сценарий 16** — просьба денег ушла в `STOP_ABUSE` вместо `STOP_MONEY_REQUEST`
     (приоритет терминальных сигналов теперь считает код — значит, промах в самом сигнале);
   - **Аналитик, сценарий 63** — ложный `STOP_TASK_REQUEST` на вопросе кандидата о задачах;
   - **Аналитик, сценарий 6 (оба варианта)** — `STOP_GIBBERISH_REPEAT` не сработал;
   - **Аналитик, сценарий 67 (оба варианта)** — `asking=salary` не случился ни разу (были `q1`, `q2`).
2. **Тракт `tgApi` не запускался ни разу** — воркер → Mongo → OpenAI, ленивая миграция на реальных
   документах, outbox. Из штаба продуктовые репозитории не гоняются, в venv `tgApi` исполнялось
   только ядро (`decide()` на собранном state, миграция на старом и новом документе). Прогнать может
   только владелец репозитория.
3. **Решить судьбу коммита `9c2faea`** в `tgApi` (правка отсева по локации, разбор —
   [review_20260831.md](review_20260831.md)): досылать в открытый PR или ждать правок по итогам
   eggplant. Сознательно не запушен до приёмки hh-контура.

## hh-контур

Порядок тот же, что на tg: сначала код, потом промпт. Гонять — `--channel hh --engine policy
--analyzer-version v3` (раннер тот же, фикстуры `tests/fixtures/screening_split_hh/`).

4. **Живой прогон hh-канала.** Офлайн-гейт (27 проверок) и тест диалогов (`--channel hh`, 4/4)
   качество промпта НЕ проверяют — наблюдение там скриптовано. `screening_analyzer_hh/v3` не мерялся
   ни разу; он же лежит в `prompts` untracked и в релиз 1.2.2 не попал.
5. **Код в `eggplant-api`** — ядро `policy` туда не переносилось, зарплатного контракта там нет
   вовсе. Переносить из `domain/screening_split_hh/policy/` (оно уже канальное) плюс async-обвязка.
   Гарды приезжают вместе с ядром: отдельной точки санитайзера в `HHClient.send_message`
   (`app/hh/client.py:129`) заводить не надо, шлюз стоит внутри движка. Вместе с ядром едут и две
   правки вежливости, сделанные на tg: вводная перед доп-вопросами (`questions_intro_sent`) и
   эскалация переспросов со слотом `ack` — обе описаны в [review_20260830.md](review_20260830.md).
   Приветствие (`greeting`/`greeted`) в hh-ядре харнесса уже есть и гейтится инвариантом
   `greeting_once` (Р16); гео-отсев переносится как есть — политика tg, ключ канальный (Р17).
6. **P11, валюта вилки в eggplant.** В ядре решено для обоих каналов: вилка типизирована
   `{min, max, currency}` и приводится к рублям (`DecideContext.band_currency`, проверено офлайн на
   вилке в тенге). В `eggplant-api` поле `currency` приходит из `hh_vacancy_data["salary"]`, и вместе
   с ядром туда надо дотащить его чтение — иначе вилка в тенге снова сравнится как рублёвая.
7. **`REASK_CAP` в eggplant** — там `2` со старой семантикой «сверяем до инкремента». При переносе
   ядра привести к `3` по новой семантике (Р3), иначе пороги каналов разъедутся.
8. **Хранилище и ленивая миграция в eggplant.** Решение принято (Р15): вилка — снапшот в строке
   диалога, одной колонкой `salary_band` (JSONB), как поле документа в tgApi; нужна одна
   Alembic-ревизия, `currency` читается из `hh_vacancy_data["salary"]`. Состав миграции старых строк —
   [handoff_eggplant.md](handoff_eggplant.md) §5.1–5.2: вилку разобрать из контекста ДО её вырезания,
   бэкфилл громкий, неразобранная вилка → `REPLY_FALLBACK` без завершения диалога (а НЕ «проходит»),
   ключи `state` добить дефолтами (JSONB, ревизии не требует), `last_asking` обнулить.
9. **Тесты hh.** Анти-луп кейсов (`counter_loops.yaml`) в hh нет вовсе — завести. Проверить, есть ли
   аналоги tg-сценариев 64/65 (инцидент 17.08 заводился на tg) и гео-ветки; инвариант
   `last_instruction_lacks` в hh стоит на одном сценарии против шести в tg.

---

## Отложено

Сознательно не сейчас — с условием возврата.

- **Колонка `engine` в tgApi** (пункт 4 паритета). Убрать нельзя: после `#105` она работает не
  как селектор движка, а как гейт для диалогов, заведённых до включения split (без `engine == "split"`
  они должны штатно закрываться, а не уходить в split-движок без state). Возвращаемся, когда таких
  диалогов не останется в Mongo — тогда гейт заменяется проверкой наличия записи со state, как в eggplant.
- **Задачи владельцев eggplant-api** (`TASKS.md`, `TECH_DEBT.md`: PR2 по кандидатам, задачи 8–12,
  событийная модель, синк снапшота резюме, воронка HH) — не наш скоуп, читаем как контекст.

## Большие задачи — после того, как закроем мелкие

Каждая требует отдельного разбора и собственного решения; в текущие пункты не встраиваются.

- **Б2. Архитектура данных пересчёта.** Парсера больше нет (распознаёт Аналитик), но остались
  `salary_rules.py` — курсы, нормо-часы, шкала НДФЛ — в **трёх копиях**, единственная защита сверка
  глазами по одному файлу. Выбор: оставить копии или общий пакет-библиотека по образцу `prompts`
  (дороже, зато один источник правды на три канала).

