# Кросс-репный план split-скрининга

> **Статус на 30.08.2026.** Документ вычищен от закрытых пунктов: всё, что закрыла перестройка
> движка, описано в [decisions_rearchitecture.md](decisions_rearchitecture.md) (нумерация Р), сам
> проект — [rearchitecture.html](rearchitecture.html), сравнение движков —
> [handoff_two_engines.md](handoff_two_engines.md). Здесь остаётся только незакрытая работа плюс
> решения, на которые ссылаются соседние документы.

Живой план: что делаем, в каком порядке и почему именно так. Файл живёт до внедрения — по мере закрытия
пунктов доказательства уходят в журнал прогона (`review_<дата>.md`), а закрытые пункты отсюда удаляются.

Статусы: **РЕШЕНО** — решение принято, можно делать · **ОБСУЖДАЕТСЯ** — решения ещё нет, нужен выбор ·
**ОТЛОЖЕНО** — сознательно не сейчас, с условием возврата.

Карта репозиториев и правила штаба — [../repos.md](../repos.md).

---

## Счётчики: семантика (одна для всех трёх портов)

«Харнесс» ниже — наш порт движка в `src/qa_harness/domain/screening_split/`, третья копия того же кода;
в проде не участвует, но именно он гейтит тесты промптов, поэтому расхождение с продом = ложный вердикт.

| Механизм | Что считает | Что инкрементит | Сброс | Эффект на пороге |
|---|---|---|---|---|
| `gibberish` | бессвязные/не по делу реплики | `event:"gibberish"` от Аналитика | нет (монотонный) | код форсит `STOP_GIBBERISH_REPEAT` |
| `bot_check` | «вы бот?» — проверки на живого человека | `event:"bot_check"` | нет | `STOP_BOT_REPEAT` |
| `demand` | **настойчивость кандидата**: одно и то же требование к нам, ультиматумы | `event:"demand"` (решение LLM, не кода) | нет | `STOP_PERSISTENT` |
| `contact_source` | требование раскрыть источник контакта | `event:"contact_source"` | нет | `STOP_PERSISTENT` |
| `pause` | просьбы отложить/перенести | `event:"pause"` | нет | `STOP_PAUSE` |
| `salary_info` | сколько раз кандидат требовал **НАШУ** вилку | `event:"salary_info"` | нет | **порога в коде нет** — читает только промпт (`0` → объяснить политику, `≥1` → задать вопрос без объяснения) |
| reask-cap (`salary_reasks`, `format_reasks`, `field_work_reasks`, `q*.reask_count`) | **повторные заходы на один и тот же незакрытый пункт** | `next_action:"ask"` + `asking == state.last_asking` + пункт всё ещё `pending` | нет; инкремента не будет, если Аналитик сменил `asking`, но накопленное сохраняется | зарплата → `STOP_SALARY_DEMAND`; формат/разъездной → `STOP_PERSISTENT`; доп-вопрос → `refused` + обнуление `last_asking` + **перерешивание хода** (второй вызов Аналитика) |
| `no_progress` | **ходы без нового факта**, независимо от темы | `progress_signature` не изменилась за ход | **да, в 0** при любом новом факте — единственный самообнуляющийся | `FINISH` если всё собрано, иначе `STOP_PERSISTENT` |

`progress_signature` = (`salary`, `format_check`, [`field_work_check`], есть ли город, статусы `q*`).
`reask_count` в неё НЕ входит: переспрос прогрессом не считается.

Пороги проверяются **до** инкремента, поэтому «порог 2» означает срабатывание **на 3-м** повторе.

## Счётчики: значения по портам

| Механизм | tgApi | eggplant (hh) | харнесс |
|---|---|---|---|
| `gibberish` | 2 | 2 | 2 |
| `bot_check` | 2 | 2 | 2 |
| `demand` | 3 | 3 | 3 |
| `pause` | 3 | 3 | 3 |
| `contact_source` | 3 | **нет** (в hh-канале нет источника контакта) | 3 |
| `salary_info` | счётчик есть, порога нет | то же | то же |
| reask `salary` | 2 → STOP на 3-м | 2 (общая `REASK_CAP`) → STOP на 3-м | 2 → STOP на 3-м (приведён к проду 2026-08-26) |
| reask `format` | 2 (своя константа) | 2 (общая `REASK_CAP`) | 2 |
| reask `field_work` | **нет ключа** | есть (канальный, мультиформат) | нет |
| reask доп-вопроса | 2 → `refused` + перерешивание | 2 | 2 |
| `no_progress` | 4 | 4 | 4 |
| `progress_signature` | salary, format, город, `q*` | **+ `field_work_check`** | как tg |

**Вывод аудита.** Расхождений в порогах между портами больше нет: отличия только канальные и
обоснованные (`contact_source` — в tg, `field_work` — в hh). Единственный аутлайер, харнесс с `>= 3`,
устранён 2026-08-26 — теперь все три порта завершают на 3-м переспросе, как и написано в обоих промптах
Аналитика.

Перекрытие зон остаётся: reask-cap (по нашему вопросу), `demand` (по метке LLM) и `no_progress` (по факту
отсутствия прогресса) ловят одно и то же «диалог не двигается» тремя разными признаками, и первые два
срабатывают за 2–3 хода, из-за чего 4-й ход без прогресса живьём почти недостижим. Разбирали, решили
**ничего не менять** (Д5, Д6): `no_progress` остаётся страховкой, `demand` — рабочим механизмом.

---

## Решения по счётчикам (без кода)

Нумерация сквозная и **с пропусками**: закрытые решения удалены, номера не переиспользуются. Д3
(приведение порога харнесса к проду) закрыто 26.08.2026 — итог в «Выводе аудита» выше. Д8–Д13
(зарплатный контракт) живут в [mechanics_decision.html](mechanics_decision.html).

- **Д1 · ОТКЛОНЕНО (28.08.2026).** Гейт reask-cap по прогрессу («не засчитывать переспрос, если ход
  дал новый факт») НЕ делаем. Рассматривали как лекарство от инцидента 2026-08-17, отказались.

  **Почему.** Кап считает не «получили ли мы ответ», а «сколько раз мы задали этот вопрос». Вопрос
  фактически был задан — то, что кандидат ответил на что-то другое, этого не отменяет.

  **И на зарплате он уже не нужен.** Ветка капа работает только пока `salary == pending`, а зарплатный
  блок стоит ВЫШЕ неё и закрывает пункт сразу, как только пришла пригодная сумма. Значит сценарий
  инцидента — кандидат дал валидный ответ и всё равно получил `STOP_SALARY_DEMAND` — сейчас невозможен
  независимо от накопленного счётчика. Накопленное вредит только тому, кто до конца диалога так и не
  назвал пригодную сумму, а это и есть уклонение.

  **Что это значит для кривых меток `asking`.** Аналитик иногда оставляет `asking:"salary"` на ходе, где
  спрашивает про город (видно в прогонах 27-28.08). Лишняя единица в счётчике при этом безвредна по той
  же причине: как только сумма названа, ветка капа больше не отрабатывает. Принято как допустимый риск.

  Следствие для тестов: регрессия инцидента 2026-08-17 отдельным кейсом **не заводится** — ловить
  нечего.

- **Д2 · РЕШЕНО.** В eggplant `REASK_CAP` глобально **не** поднимаем. Отдельный порог для зарплаты
  тоже не нужен: после переноса зарплатного блока выше капа валидный ответ закрывает пункт раньше,
  чем счётчик успевает что-то решить (см. Д1).
- **Д4 · РЕШЕНО.** `salary_info` с reask-счётчиком **не сливаем** — они считают разное:
  `salary_info` = сколько раз кандидат требовал нашу вилку, reask = сколько раз мы переспросили сумму.
  Слияние ломает политику в обе стороны: (1) кандидат дважды требует вилку, но между требованиями
  Аналитик сменил `asking` → reask не вырос → объяснение повторится; (2) кандидат вилку не просил, но
  дважды дал неоднозначную сумму → reask ≥ 1 → промпт сочтёт политику объяснённой, хотя кандидат её не
  слышал. Плюс жёсткая связка: reask гейтит завершение, и правка формулировки двигала бы порог STOP.
- **Д5 · РЕШЕНО.** `no_progress` оставляем как страховку, значение 4 не трогаем.
- **Д6 · РЕШЕНО: `demand` оставляем как есть.** Идею заменить его счётчиком отсутствия прогресса
  (и снизить `NO_PROGRESS_CAP` до 3) разобрали и закрыли: механизм работает, выигрыш — только
  косметический (минус один LLM-зависимый счётчик), а трогать пришлось бы завершение диалогов во всех
  трёх портах. Не переоткрывать без нового повода.
- **Д7 · РЕШЕНО.** Монотонность счётчиков (никто, кроме `no_progress`, не сбрасывается) — **свойство,
  а не дефект**: счётчик показывает, сколько раз мы фактически задали этот вопрос за весь диалог, а не
  «сколько подряд». Именно это делает диалог конечным. Обнуляется только `no_progress`; остальные
  счётчики растут через весь диалог — и это не дефект.

---

## Открытая работа

### Блокирует релиз нового движка

1. **Прогон `--input-mode generated`** — не делался ни разу. Scripted проверяет фиксированные реплики,
   вариативные формулировки не проверены вообще.
2. **Запуск порта в среде tgApi** — код написан и статически сверен, но не исполнялся. Из QA-стенда
   продуктовые репозитории не гоняются.
3. **Перепроверить сценарии 24 и 55** — падали стабильно, упирались в рубрику судьи; после её правки
   (Р12) прогон не повторялся.
4. **Релиз пакета `prompts`** — bump версии + GitHub Release / GHCR-образ. **Порядок обязателен:
   сначала код в master, потом релиз промпта.** Код терпит отсутствие поля, промпт без кода отдавал бы
   наблюдение в пустоту. К релизу `screening_analyzer` переключается на **v3** (`pointer.yaml` сейчас
   `active: v2`).

### hh-контур (после приёмки tg)

Порядок тот же, что на tg: сначала код, потом промпт. Гонять — `--channel hh` (раннер тот же, фикстуры
`tests/fixtures/screening_split_hh/`).

5. **`screening_analyzer_hh` v2** — там полный зарплатный блок с арифметикой и правила, ПРЯМО
   противоречащие принятым для tg: диапазон по верхней границе (против Д13), голое «260» уточнять
   (против Д8), gross с вилкой не сравнивать (против Д11). Пока не перенесено — прогоны каналов
   несравнимы. Переносим не правки v2, а **контракт `Observation`**: hh-аналог `screening_analyzer` v3.
   Канальные отличия, которые нельзя потерять: мультиформат, `field_work`, `KO_LOCATION_GEO` (Б3).
6. **Код в `eggplant-api`** — зарплатного контракта там нет вовсе; ядро `policy` тоже не переносилось.
   Вместе с ядром приезжают гарды: отдельной точки санитайзера в `HHClient.send_message`
   (`app/hh/client.py:129`) заводить не надо, шлюз стоит внутри движка.
7. **P11, валюта вилки.** Для tg решено: вилка типизирована `{min, max, currency}` и приводится к
   рублям. Для hh остаётся — поле `currency` приходит из `hh_vacancy_data["salary"]`, но не читается,
   и вилка в тенге сравнивается как рублёвая.
8. **`REASK_CAP` в eggplant** — там `2` со старой семантикой «сверяем до инкремента». При переносе
   ядра привести к `3` по новой семантике (Р3), иначе пороги каналов разъедутся.
9. **Тесты hh.** Анти-луп кейсов (`counter_loops.yaml`) в hh нет вовсе — завести. Проверить, есть ли
   аналоги tg-сценариев 64/65 (инцидент 17.08 заводился на tg) и гео-ветки; инвариант
   `last_instruction_lacks` в hh стоит на одном сценарии против шести в tg.

### Промпты

10. **Чистка длинных тире в телах промптов.** Правило в промпт не добавляем — запрет живёт в гарде G0;
    из промптов делаем только обратное, убираем источник прайминга (модель копирует пунктуацию из
    примеров). Осталось: `screening_analyzer` v3 — 88 вхождений, `screening_analyzer_hh` v2 — 176,
    `screening_interviewer_hh` v1 — 42. `screening_interviewer` v2 уже чистый. Замена механическая,
    едет вместе с ближайшим релизом соответствующего компонента; на поведение не влияет — G0 переписывает
    тире на исходящем тексте в любом случае.

---

## Тесты

Что есть сейчас — tg: `scenarios.csv` 95 строк, `scenario_checks.yaml` 285, `candidate_inputs.yaml` 346,
`counter_loops.yaml` 9 кейсов. hh (`tests/fixtures/screening_split_hh/`, раннер `--channel hh`):
`scenarios.csv` 61, `scenario_checks.yaml` 247, `candidate_inputs.yaml` 294, анти-луп кейсов нет.

**Правила прогонов (из опыта 2026-08-26).**
- **Точечный прогон против полного.** 6 сценариев = 38 с и 224 тыс. токенов (перекос в input: system
  Аналитика ~38 тыс. символов уходит в каждый вызов). Полный гейт на 67 сценариях — порядка 2,5 млн
  input-токенов, поэтому он не для итераций: гоняем точечно по затронутым индексам, а полный — один раз
  как приёмка перед релизом пакета.
- **Суммы в новых фикстурах — только плейсхолдерами** (`{in_band}` / `{above_max}` / `{below_min}`),
  никогда абсолютным числом: вилку задаёт вакансия сценария. Сценарий 67 сначала падал на законном
  `KO_SALARY`, потому что «300000 на руки» превышало дефолтную вилку 200000-280000, — тест ловил
  правильное поведение как ошибку.
- **Один и тот же дефект умеет «переезжать» между сценариями.** Выдуманный URL 22.08 падал на 64,
  26.08 — на 66; сценарии делят вакансию и похожие реплики. Вывод: сравнивать прогоны по КЛАССУ дефекта,
  а не по номеру сценария, иначе «64 позеленел» читается как исправление.

---

## Отложено

- **Колонка `engine` в tgApi** (пункт 4 паритета) · ОТЛОЖЕНО. Убрать нельзя: после `#105` она работает не
  как селектор движка, а как гейт для диалогов, заведённых до включения split (без `engine == "split"`
  они должны штатно закрываться, а не уходить в split-движок без state). Возвращаемся, когда таких
  диалогов не останется в Mongo — тогда гейт заменяется проверкой наличия записи со state, как в eggplant.
- **Задачи владельцев eggplant-api** (`TASKS.md`, `TECH_DEBT.md`: PR2 по кандидатам, задачи 8–12,
  событийная модель, синк снапшота резюме, воронка HH) — не наш скоуп, читаем как контекст.

## Большие задачи — после того, как закроем мелкие

Каждая требует отдельного разбора и собственного решения; в текущие фазы не встраиваются.

- **Б1. Плавный переход к дополнительным вопросам** (сначала tgApi). Сейчас после закрытия приоритетов
  Аналитик сразу задаёт `q1`, о котором кандидата никто не предупреждал: с его стороны разговор про
  зарплату и формат внезапно превращается в опрос по навыкам. Нужна вводная фраза — **ровно один раз
  за диалог**, в момент перехода; при переспросе `q1` и на всех следующих вопросах её быть не должно.
  - **Черновик формулировки** (уточнить): «Хочу задать несколько дополнительных вопросов от команды —
    они нужны, чтобы подтвердить навыки и квалификацию. Ответьте, пожалуйста, сами, без ИИ и поиска
    в интернете: нам важно оценить ваши реальные знания.»
  - **Ограничения, которые нельзя нарушить.** Вводная — это утверждение, а НЕ вопрос: формулировка
    «готовы ответить на несколько коротких вопросов?» промптом прямо запрещена, и правило «в
    `instruction` ровно ОДИН вопрос из [questions]» тоже остаётся в силе. То есть ход выглядит так:
    вводная фраза + сам `q1`, без «можно?» и без перечисления вопросов списком.
  - **Почему одним промптом это не решается надёжно.** Признака «вводную уже сказали» в состоянии нет.
    `reask_count` не годится: если между двумя заходами на `q1` фокус сменился (ответили на вопрос
    кандидата), код переспрос не засчитает, счётчик останется `0`, и промпт решит, что переходит к
    вопросам впервые — вводная повторится. Ровно тот случай, который надо исключить.
  - **Вариант А (рекомендуемый).** Флаг в состоянии — `questions_intro_sent`, ставит КОД в тот ход,
    когда фокус впервые оказался ключом доп-вопроса; текст вводной приезжает слотом `convey` из
    `_ask_slot`/`_convey_slot`. Повтор исключён кодом, а не дисциплиной модели.
  - **Вариант Б.** Код сам приклеивает канонический текст вводной перед репликой Интервьюера на том же
    ходе. Формулировка гарантированно дословная, но сообщение склеивается из двух частей и может
    читаться рвано. Держим как запасной, если Интервьюер начнёт перевирать вводную.
  - **Открытый вопрос:** делать ли симметрично в hh — там тот же резкий переход в приоритете 4.
    Скорее да: каналы держим сходящимися, и правка одинаковая.
  - **Тест:** сценарий «зарплата и формат закрыты → первый доп-вопрос» — вводная присутствует;
    следующий ход с переспросом `q1` — вводной НЕТ; третий доп-вопрос — вводной НЕТ. Детерминированный
    инвариант по трассе, судья не нужен.
- **Б2. Архитектура данных пересчёта.** Парсера больше нет (распознаёт Аналитик), но остались
  `salary_rules.py` — курсы, нормо-часы, шкала НДФЛ — в **трёх копиях**, единственная защита сверка
  глазами по одному файлу. Выбор: оставить копии или общий пакет-библиотека по образцу `prompts`
  (дороже, зато один источник правды на три канала).
- **Б3. Окончательное решение по кандидатам за границей.** Сейчас единой политики нет. В hh остался
  рудимент: `KO_LOCATION_GEO` отсеивает только при двойном совпадении — кандидат явно сказал, что он
  за рубежом, И в вакансии явно прописано гео-ограничение. Это работает случайно, а не по правилу.
  Решить: либо убираем механику совсем (кандидатов за границей не отсеиваем на скрининге), либо
  доделываем до внятного правила. В tg безусловный отсев уже снят: `STOP_ABROAD` не существует,
  остаётся условный `KO_GEO` по явному гео-ограничению вакансии.
